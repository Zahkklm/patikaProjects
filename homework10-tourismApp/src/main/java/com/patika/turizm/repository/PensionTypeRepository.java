package com.patika.turizm.repository;

import com.patika.turizm.model.PensionType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PensionTypeRepository extends JpaRepository<PensionType, Long> {
}
